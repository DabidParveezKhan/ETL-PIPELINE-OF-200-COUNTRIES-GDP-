# ETL-PIPELINE-OF-200-COUNTRIES-GDP-
This project implements an end-to-end ETL pipeline in Python to extract GDP data of around 200 countries from Wikipedia, transform it into USD billions, and load it into CSV and SQLite database formats.
